// Minimal placeholder for NextAuth / Supabase
// You can replace with real Supabase auth keys
export default function handler(req, res) {
  res.status(200).json({ message: "Auth endpoint placeholder" });
}
